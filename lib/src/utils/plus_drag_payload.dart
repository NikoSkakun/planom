/// Marker payload dragged by the global Plus button. The drag target's type
/// determines what gets created when the user releases the button there —
/// see `DragTarget<PlusDragPayload>` instances in Tasks, Notes and Calendar.
class PlusDragPayload {
  const PlusDragPayload();
}
